[workspace]
resolver = "2"
members = ["crates/kiry-core", "crates/kiry"]

[workspace.package]
version = "0.1.0"
edition = "2021"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
opt-level = 3
